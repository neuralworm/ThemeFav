import * as lib from './lib'
import { IThemeEXT } from './models/IThemeExtJSON'

type LocationType = "uncategorized" | "folder" | "installed"
export const transferFromTo = (from: LocationType, to: LocationType, theme: IThemeEXT) => {
    
}